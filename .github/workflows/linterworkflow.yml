name: checkLinter
on: 
  - push

jobs:
  build:
    
    runs-on: macos-latest
    
    strategy:
      matrix:
        node-version: [13.x]
  
    
    steps:
      - uses: actions/checkout@v1
      - name: Run linter
        run: |
          make install
          make lint
          
